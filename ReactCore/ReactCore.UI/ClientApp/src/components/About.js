import * as React from 'react';
import { Card,  CardText, CardBody } from 'reactstrap';

const About = () => (
    <Card>
        <CardBody>
            <CardText>This application demonstrates the use of React with .NET Core.</CardText>
        </CardBody>
    </Card>
)

export default About;